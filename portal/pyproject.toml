[tool.poetry]
name = "portal"
version = "0.1.0"
description = "Внутренний портал"
authors = ["Aleksandr Melnikov <melnikovav@med-to.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
